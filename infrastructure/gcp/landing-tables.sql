-- Run in BigQuery, processing location: asia-northeast1.
-- Existing tables are not altered by IF NOT EXISTS; compare schema if already present.

CREATE TABLE IF NOT EXISTS `trade-analytics-508604.trade_landing.day05_partner_detail_202301_r1` (
  `typeCode` STRING,
  `freqCode` STRING,
  `refPeriodId` INT64,
  `refYear` INT64,
  `refMonth` INT64,
  `period` STRING,
  `reporterCode` INT64,
  `reporterISO` STRING,
  `reporterDesc` STRING,
  `flowCode` STRING,
  `flowDesc` STRING,
  `partnerCode` INT64,
  `partnerISO` STRING,
  `partnerDesc` STRING,
  `partner2Code` INT64,
  `partner2ISO` STRING,
  `partner2Desc` STRING,
  `classificationCode` STRING,
  `classificationSearchCode` STRING,
  `isOriginalClassification` BOOL,
  `cmdCode` STRING,
  `cmdDesc` STRING,
  `aggrLevel` INT64,
  `isLeaf` BOOL,
  `customsCode` STRING,
  `customsDesc` STRING,
  `mosCode` STRING,
  `motCode` INT64,
  `motDesc` STRING,
  `qtyUnitCode` INT64,
  `qtyUnitAbbr` STRING,
  `qty` STRING,
  `isQtyEstimated` BOOL,
  `altQtyUnitCode` INT64,
  `altQtyUnitAbbr` STRING,
  `altQty` STRING,
  `isAltQtyEstimated` BOOL,
  `netWgt` STRING,
  `isNetWgtEstimated` BOOL,
  `grossWgt` STRING,
  `isGrossWgtEstimated` BOOL,
  `cifvalue` STRING,
  `fobvalue` STRING,
  `primaryValue` STRING,
  `legacyEstimationFlag` INT64,
  `isReported` BOOL,
  `isAggregate` BOOL
);

CREATE TABLE IF NOT EXISTS `trade-analytics-508604.trade_landing.day05_world_total_202301_r1` (
  `typeCode` STRING,
  `freqCode` STRING,
  `refPeriodId` INT64,
  `refYear` INT64,
  `refMonth` INT64,
  `period` STRING,
  `reporterCode` INT64,
  `reporterISO` STRING,
  `reporterDesc` STRING,
  `flowCode` STRING,
  `flowDesc` STRING,
  `partnerCode` INT64,
  `partnerISO` STRING,
  `partnerDesc` STRING,
  `partner2Code` INT64,
  `partner2ISO` STRING,
  `partner2Desc` STRING,
  `classificationCode` STRING,
  `classificationSearchCode` STRING,
  `isOriginalClassification` BOOL,
  `cmdCode` STRING,
  `cmdDesc` STRING,
  `aggrLevel` INT64,
  `isLeaf` BOOL,
  `customsCode` STRING,
  `customsDesc` STRING,
  `mosCode` STRING,
  `motCode` INT64,
  `motDesc` STRING,
  `qtyUnitCode` INT64,
  `qtyUnitAbbr` STRING,
  `qty` STRING,
  `isQtyEstimated` BOOL,
  `altQtyUnitCode` INT64,
  `altQtyUnitAbbr` STRING,
  `altQty` STRING,
  `isAltQtyEstimated` BOOL,
  `netWgt` STRING,
  `isNetWgtEstimated` BOOL,
  `grossWgt` STRING,
  `isGrossWgtEstimated` BOOL,
  `cifvalue` STRING,
  `fobvalue` STRING,
  `primaryValue` STRING,
  `legacyEstimationFlag` INT64,
  `isReported` BOOL,
  `isAggregate` BOOL
);
